# Codeception Test Suite Configuration
#
# Suite for unit (internal) tests.

class_name: UnitTester
coverage:
  enabled: true
  remote: false
  include:
    - models/*
    - services/*
    - lib/*
modules:
  enabled:
    - Asserts
    - \Helper\Unit
