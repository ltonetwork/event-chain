<?xml version="1.0"?>
<ruleset name="PSR">
    <description>PSR-1 and PSR-2</description>
 
    <!-- Include the whole PSR-1 standard -->
    <rule ref="PSR1">
        <exclude name="PSR1.Classes.ClassDeclaration"/>
    </rule>

    <!-- Include the whole PSR-2 standard -->
    <rule ref="PSR2"/>
</ruleset>

